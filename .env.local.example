# No secrets needed — all data is fetched from public GCS URLs
# Copy to .env.local if you need to override defaults

# Optional: Override the GCS feed URL (defaults to production)
# NEXT_PUBLIC_GCS_BASE=https://storage.googleapis.com/cleartext-podcast
